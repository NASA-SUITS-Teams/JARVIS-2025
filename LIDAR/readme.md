after plotting all the points on a 3d graph this is what it looks like:
https://www.desmos.com/3d/ilym8c4y1r

Set up LIDAR processing by installing the following packages:
pip install open3d
pip install numpy

pointcloudvisualizer visualizes a point cloud data file
tait-brayant-angles-demo.py is a demo of how we would use tait-bryan angles to transform LiDAR sensor data to point in global coordinates, currently not functional.